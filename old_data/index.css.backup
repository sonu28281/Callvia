@import url('https://fonts.googleapis.com/css2?family=Sora:wght@400;500;600;700&family=Inter:wght@300;400;500;600;700&display=swap');

@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  html, body, #root {
    width: 100%;
    height: 100%;
  }

  body {
    font-family: 'Inter', system-ui, -apple-system, sans-serif;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    background-color: #070A12;
    color: #EAF0FF;
    line-height: 1.6;
  }

  h1, h2, h3, h4, h5, h6 {
    font-family: 'Sora', system-ui, sans-serif;
    font-weight: 600;
    letter-spacing: -0.02em;
    line-height: 1.2;
  }

  a {
    text-decoration: none;
    transition: color 200ms ease-out;
  }
}

@layer utilities {
  /* Professional micro-interactions only */
  .transition-smooth {
    transition: all 200ms ease-out;
  }
  
  .transition-fast {
    transition: all 150ms ease-out;
  }

  /* Subtle hover lift for cards */
  .card-hover:hover {
    transform: translateY(-2px);
  }

  /* Professional focus states */
  .focus-ring:focus {
    outline: 2px solid #FF9F1C;
    outline-offset: 2px;
  }

  /* Routing lines decoration (very subtle) */
  .routing-lines {
    background-image: 
      linear-gradient(90deg, rgba(34, 48, 85, 0.08) 1px, transparent 1px),
      linear-gradient(rgba(34, 48, 85, 0.08) 1px, transparent 1px);
    background-size: 40px 40px;
    opacity: 0.5;
  }
}

@media (prefers-reduced-motion: reduce) {
  * {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
